import React from 'react';
import styles from './ContentEditable.module.sass';

const ContentEditable = props => {
  return (
    <div className={ styles.content_editable }>
      <div> </div>
    </div>
  )
}

export default ContentEditable;